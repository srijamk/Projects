import java.lang.Scanner;

public boolean frontAgain(String str) {
  if (str.length() < 2) {
    return false;
  } else if (str.length() == 2) {
    return true;
  } else {
    return str.substring(0, 2).equals(str.substring(str.length() - 1));
  }
}
