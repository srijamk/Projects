import java.io.*;
import static java.lang.System.*;
import java.util.Scanner;

class Lesson_22 {
  
    public static void main (String str[]) throws IOException {
        Scanner scan = new Scanner (System.in);
        /*
        String a = "apple";
        String b = "banana";
        System.out.println("apple equals banana? " + a.equals(b));
        System.out.println("fifth character of apple? " + a.charAt(4));
        System.out.println("index of 'b' in banana? " + b.indexOf('b'));
        System.out.println("substring of banana from index 2 - 5? " + b.substring(0, 3));
        System.out.println("word in banana starting at index 2? " + b.substring(2));   
        System.out.println("banana in uppercase? " + b.toUpperCase());
        System.out.println("apple compared to banana in alphabet? " + a.compareTo(b));
        System.out.println("Enter a string: ");
        */
          }
        }
    }

